This is my Bronze Tier AI Employee system. It automatically reads tasks, processes them using AI, and generates structured plans using predefined skills.

This system uses an Obsidian vault as its brain. Tasks flow from Inbox to Needs_Action, then AI processes them and stores results in Plans.

All AI behavior is controlled using agent skills. These define how the AI thinks and responds.

The orchestrator runs continuously and checks for new tasks every 15 seconds.

Now I will give a task to the AI employee.

The system detects the task, enriches  it with skills, and sends it to the AI model.

The AI generates a structured execution plan and stores it automatically.

After processing, the task is moved to Done, making this a complete automated workflow.

This system is not just generating text, it is executing a workflow like a real employee.
The AI uses predefined skills to ensure structured and domain-specific outputs.
The entire pipeline is automated — from task input to execution planning.

Thank You!